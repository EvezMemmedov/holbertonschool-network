#!/usr/bin/env bash
# File: 4-TCP_and_UDP_ports
# Description: Display listening TCP/UDP ports and UNIX sockets with PID/Program name

echo "Active Internet connections (only servers)"
# TCP and UDP listening ports with PID/program
netstat -tulnp 2>/dev/null | grep LISTEN

echo
echo "Active UNIX domain sockets (only servers)"
# UNIX domain sockets in LISTEN state with PID/program
netstat -xlnp 2>/dev/null

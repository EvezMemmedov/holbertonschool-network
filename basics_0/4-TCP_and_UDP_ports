#!/usr/bin/env bash
# 4-TCP_and_UDP_ports
# Displays listening TCP/UDP ports and UNIX sockets with PID/Program name

echo "Active Internet connections (only servers)"
sudo netstat -tulpn | grep LISTEN

echo ""
echo "Active UNIX domain sockets (only servers)"
sudo netstat -lxpn | grep LISTEN
